# Dependências leves — instalação rápida, usadas por quase todos os módulos.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pyyaml>=6.0
regex>=2024.5.15
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27
nbformat>=5.10

# V2 — differential_privacy (mecanismo de Laplace)
numpy>=1.26

# V3/V4 (extração real) — behavioral_monitoring (teste KS), constitution_compiler
# e regulatory_simulation reusam networkx (já listado em requirements-heavy.txt)
scipy>=1.13
